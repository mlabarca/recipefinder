<%= form_tag(recipes_index_path, method: "get") do |f| %>
  <%= label_tag(:search, "Search recipes mentioning:") %>
  <%= text_field_tag(:search, @search_term) %>
  <%= submit_tag("Search") %>
<% end %>